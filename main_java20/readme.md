#Basic HashMap Operations

                                        Introduction To HashMap

 Definition:-
 A hashmap stores key-value pairs using a  hash table. it allows one null key and multiple null values.

 Advantages:-
 . Fast average-time performance for get and put operations(0(1)).
 . Unuque keys:-
 Inserting a dublicate key replace its value.

 Common Methods:-
. Put, 
value, 
get(key) ,
 remove(key) , 
 containskey(),
  containsvalue(),
   keySet(), 
   values(), 
   entrySet(),
    size(), 
    clear().

Use Caese:-
. Coaching ,
  Configuration settings,
  dictionary implementation etc.